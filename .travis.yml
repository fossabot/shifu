language: go

go:
  - 1.6

install:
  - go get gopkg.in/yaml.v2
  - go get net/http
  - go get github.com/docopt/docopt-go

before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash) -t 4fb48463-40b8-43ef-b579-d4c349f84d8b
